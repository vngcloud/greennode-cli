# list-examples

## Description

List example __DISPLAY__ resources. (Starter command — replace with your real command docs, following the AWS-CLI-style reference format used by the other commands.)

## Synopsis

```
grn __PRODUCT__ list-examples
    --parent-id <value>
```

## Options

**`--parent-id`** (string)

Parent resource ID.

- Required: Yes

## Global options

This command also accepts the global options (`--profile`, `--region`, `--output`, `--query`, `--endpoint-url`, `--debug`, …).

## Examples

```bash
grn __PRODUCT__ list-examples --parent-id p-abc12345
```
